# 用于控制IF…ELSE…语句的书写方式
SET(CMAKE_ALLOW_LOOSE_LOOP_CONSTRUCTS ON)

ADD_EXECUTABLE(curltest main.c)

FIND_PACKAGE(CURL)
IF(CURL_FOUND)
	INCLUDE_DIRECTORIES(${CURL_INCLUDE_DIR})
	TARGET_LINK_LIBRARIES(curltest ${CURL_LIBRARY})
ELSE()
	MESSAGE(FATAL_ERROR "CURL library not find!")
ENDIF()
